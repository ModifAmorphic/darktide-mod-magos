# Magos Modificus: builds the .NET 10 + Avalonia 12 mod-manager app.
# Scoped to magos-modificus/** (the Enginseer runtime lives in a separate
# repo: https://github.com/ModifAmorphic/darktide-enginseer).
name: magos-build

on:
  push:
    branches: [main]
    paths:
      - 'magos-modificus/**'
      - '.github/workflows/magos-build.yml'
  pull_request:
    branches: [main]
    paths:
      - 'magos-modificus/**'
      - '.github/workflows/magos-build.yml'
  workflow_dispatch:

jobs:
  build:
    name: build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    defaults:
      run:
        working-directory: magos-modificus

    steps:
      - uses: actions/checkout@v7

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore magos-modificus.sln

      - name: Build (Release)
        run: dotnet build magos-modificus.sln --configuration Release --no-restore

      - name: Test (Release)
        run: dotnet test magos-modificus.sln --configuration Release --no-build --logger trx

      - name: Publish (framework-dependent)
        run: dotnet publish ui --configuration Release --output app-publish

      - name: Upload app
        uses: actions/upload-artifact@v7
        with:
          name: magos-app-${{ matrix.os }}
          path: magos-modificus/app-publish/
