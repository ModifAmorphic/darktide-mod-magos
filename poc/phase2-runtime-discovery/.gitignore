# Build artifacts (regenerated by build.sh).
build/
output/

# Capstone's in-tree build dir (regenerated by build.sh's cmake step).
vendor/capstone/build/

# Editor / OS noise.
*.swp
.DS_Store
*~

# The venv from Phase 0 is sibling to this dir, not under it; ignore any
# accidental local venv here too.
.venv/
